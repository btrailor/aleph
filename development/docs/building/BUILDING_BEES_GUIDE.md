# Building BEES Firmware

This guide has been merged into the canonical build document at the repository root:

**→ [BUILD.md](../../../BUILD.md)**

The old content here described manual `libavr32` branch management and a dangling `02469a2` pin that no longer applies. The current `develop` branch builds reproducibly from a clean GitHub clone using the instructions in `BUILD.md`.
